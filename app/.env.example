# Copy to .env (gitignored) and fill in to enable real LLM scoring + chat.
# Without this, the app runs fine on deterministic fallbacks -- see app/README.md.
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-5-sonnet-latest
